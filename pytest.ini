[pytest]
; As funções do app usam nomes em português ("teste_t_1amostra") que o padrão
; "test*" do pytest coletaria por engano; exigimos o prefixo exato "test_".
python_functions = test_*
testpaths = tests
